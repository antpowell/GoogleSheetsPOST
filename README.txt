This program is an example on how to store data from a user created form
into a Google Sheets spreadsheet.

It also shows how to set up a CTA (Call To Action) that will require information
from the user via a modal before allowing them to progress forward from their
current position.